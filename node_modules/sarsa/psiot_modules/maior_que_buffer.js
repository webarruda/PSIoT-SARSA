// Verifica se buffer "estourou", caso positivo iniciam as perdas de pacotes

function maiorquebuffer(x, tam_buffers) {

  if (x > tam_buffers) {
    return true;
  } else { return false };
}

module.exports = maiorquebuffer;